# This file is intentionally a placeholder for the post-DNS TLS transition.
# Run certbot with --nginx and the vaulted email only after HTTP works:
# certbot --nginx -d nmfdev.web.id -d ai-proxy.nmfdev.web.id --agree-tos --email "$VAULT_EMAIL" --non-interactive --redirect
